# --- Ingestion / Parsing ---
pymupdf==1.24.10          # parse PDF, giữ layout để tách công thức/bảng
markdown-it-py==3.0.0     # parse docs dạng Markdown
beautifulsoup4==4.12.3    # parse HTML docs (nếu crawl từ web)

# --- Embedding / Vector Store ---
sentence-transformers==3.0.1   # BAAI/bge-small-en-v1.5, bge-reranker
chromadb==0.5.5                 # vector store local
rank-bm25==0.2.2                 # sparse retrieval cho hybrid search

# --- LLM clients (100% free) ---
ollama==0.3.1              # chạy LLM local khi dev (Qwen2.5-3B-Instruct...), không giới hạn, không tốn phí
groq==0.11.0                # free tier, dùng khi deploy demo public (HF Spaces free CPU không chạy nổi Ollama)

# --- Evaluation ---
ragas==0.1.16              # đo Faithfulness, Context Precision/Recall, Answer Relevancy
datasets==2.20.0           # RAGAS cần định dạng HF Dataset
langchain-ollama==0.2.0    # LLM judge cho RAGAS, chạy free qua Ollama
langchain-huggingface==0.1.0  # embeddings wrapper cho RAGAS

# --- App / Serving ---
fastapi==0.112.0
uvicorn==0.30.5
gradio==4.42.0
pydantic==2.8.2

# --- Utils ---
python-dotenv==1.0.1
tqdm==4.66.5
numpy==1.26.4
pandas==2.2.2
